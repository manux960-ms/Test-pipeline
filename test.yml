name: Pipeline Studio

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "Hello — pipeline attiva!"

      - name: Simulate test
        run: echo "1 + 1 = $((1+1))"
